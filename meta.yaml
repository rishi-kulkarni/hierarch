{% set name = "hierarch" %}
{% set version = "1.1.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/hierarch-{{ version }}.tar.gz
  sha256: 595d53486be246d51eb8a1c00243d139a0cc320cf9518eb51246d2bfd60b498c

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - numba >=0.53.1
    - numpy >=1.20.2
    - pandas >=1.2.4
    - python >=3.7
    - scipy >=1.6.2

test:
  imports:
    - hierarch
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/rishi-kulkarni/hierarch
  summary: Hierarchical hypothesis testing library
  license: MIT
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - rishi-kulkarni
